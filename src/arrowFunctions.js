const names = ['Diego', 'Juan', 'Roberto'];

const num_char = names.map(function(name) {
	console.log(`${name} have ${name.length} letters`);
});

// const af_num_char = names.map((name) => {
// 	`${name} have ${name.length} letters`;
// });

// console.log(af_num_char);
